@{
    ViewData["Title"] = "Profile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@{
    ViewData["NavSection"] = "User";
}

<!-- Scoped styles for Profile only -->
<style>
    .profile .form-control, .profile select {
        background-color: var(--surface-color) !important;
        color: var(--text-color) !important;
        border: 1px solid var(--border-color) !important;
        border-radius: 8px !important;
        padding: 10px 12px !important;
        width: 100%;
    }

        .profile .form-control::placeholder {
            color: var(--muted-text-color) !important;
        }

        .profile .form-control:focus, .profile select:focus {
            outline: none !important;
            border-color: var(--primary-color) !important;
            box-shadow: 0 0 0 0.15rem var(--accent-color) !important;
        }

    .profile .section-title {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .profile .readonly input, .profile .readonly select {
        pointer-events: none;
        opacity: 0.9;
    }
</style>

<div class="profile">

    <!-- Header / Nav -->
    <div class="d-flex justify-content-between align-items-center mb-16">
        <h1 class="mb-0">Your Profile</h1>
        <div class="d-flex gap-2">
            <a asp-controller="Home" asp-action="Dashboard" class="btn btn-secondary">← Back to Dashboard</a>
            <a asp-controller="Home" asp-action="Events" class="btn btn-outline-secondary">Events</a>
            <a asp-controller="Home" asp-action="Points" class="btn btn-outline-secondary">Points &amp; Receipts</a>
            <a asp-controller="Home" asp-action="FileLibrary" class="btn btn-outline-secondary">File Library</a>
        </div>
    </div>

    <!-- Profile Overview (registration date only) -->
    <div class="card shadow p-24 mb-16">
        <h3 class="mb-8">Profile Overview</h3>
        <div class="caption text-muted">Registration Date: [ ... ]</div>
    </div>

    <!-- Editable basic info -->
    <div class="card shadow p-24 mb-16">
        <div class="section-title">
            <h3 class="mb-0">Basic Information</h3>
            <div class="d-flex gap-2">
                <button type="button" class="btn btn-sm btn-outline-secondary" disabled>Enable Edit (UI only)</button>
                <button type="button" class="btn btn-sm btn-primary" disabled>Save Changes</button>
            </div>
        </div>

        <div class="mt-16 readonly">
            <div class="row">
                <div class="col-md-6 mb-16">
                    <label for="FirstName" class="form-label">First Name</label>
                    <input id="FirstName" type="text" class="form-control" placeholder="[ First Name ]" />
                </div>
                <div class="col-md-6 mb-16">
                    <label for="Surname" class="form-label">Surname</label>
                    <input id="Surname" type="text" class="form-control" placeholder="[ Surname ]" />
                </div>
            </div>

            <div class="mb-16">
                <label for="Username" class="form-label">Username</label>
                <input id="Username" type="text" class="form-control" placeholder="[ Username ]" />
                <div class="caption mt-8">Displayed publicly on the leaderboard (if visibility enabled).</div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-16">
                    <label for="Phone" class="form-label">Phone</label>
                    <input id="Phone" type="text" class="form-control" placeholder="[ +27 ... ]" />
                </div>
                <div class="col-md-6 mb-16">
                    <label for="Email" class="form-label">Email</label>
                    <input id="Email" type="email" class="form-control" placeholder="[ you@example.com ]" />
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-16">
                    <label for="Age" class="form-label">Age</label>
                    <input id="Age" type="number" class="form-control" placeholder="[ ... ]" />
                </div>
                <div class="col-md-6 mb-16">
                    <label for="Subject" class="form-label">Subject (optional)</label>
                    <input id="Subject" type="text" class="form-control" placeholder="[ e.g., Maths ]" />
                </div>
            </div>
        </div>
    </div>

    <!-- Preferences (trimmed per spec) -->
    <div class="card shadow p-24 mb-16">
        <h3 class="mb-0">Preferences</h3>
        <div class="mt-16">
            <!-- Leaderboard visibility ONLY -->
            <div class="mb-16">
                <div class="form-check form-switch">
                    <input class="form-check-input" type="checkbox" id="LeaderboardVisibility" disabled>
                    <label class="form-check-label" for="LeaderboardVisibility">Show me on the leaderboard</label>
                </div>
                <div class="caption text-muted mt-8">Controls your public visibility on leaderboards.</div>
            </div>

            <!-- Theme preference: Light / Dark (no System) -->
            <div class="mb-0">
                <label class="form-label">Theme</label>
                <div class="d-flex gap-3">
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="themePref" id="themeLight" disabled>
                        <label class="form-check-label" for="themeLight">Light</label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="themePref" id="themeDark" disabled>
                        <label class="form-check-label" for="themeDark">Dark</label>
                    </div>
                </div>
                <div class="caption text-muted mt-8">In future, this will set your default theme.</div>
            </div>
        </div>
    </div>
</div>

@* --------------------------------------------------------------------
   TODO: Enable Profile Functionality
   - Bind profile data; persist edits via POST
   - Persist preferences: leaderboard visibility, theme (light/dark)
   - Auth guard: redirect to Login if not signed in
--------------------------------------------------------------------- *@
